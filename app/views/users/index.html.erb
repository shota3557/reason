<h1>ユーザ一覧</h1>

<table>
  <thead>
    <tr>
      <th>ユーザ名</th>
      <th>管理者権限</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.admin %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'タスク一覧へ', tasks_path %>
